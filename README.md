# terraform-aws-s3-bucket

A simple Terraform module to create an AWS S3 bucket.

## Usage

```hcl
module "s3_bucket" {
  source      = "BofA/s3-bucket-drs/aws"
  bucket_name = "my-bucket"
}
``` # terraform-aws-s3-bucket-drs
# terraform-aws-s3-bucket-drs
